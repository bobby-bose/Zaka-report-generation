### ARCHITECTURAL CHANGE: FLAT ROW SYSTEM
The system must move from a 'Nested Box' structure to an 'Independent Row' structure. Every entry in the packing list must be treated as a standalone record with its own dimensions and weights.

### 1. BACKEND MODIFICATIONS (@app.route('/api/packaging-list/create'))
- REMOVE NESTED LOOP: The current backend expects 'item['boxes']'. Remove this.
- DIRECT CALCULATION: Calculate total weights by iterating directly through 'items_data'.
- UPDATE LOGIC:
    for item in items_data:
        net = item.get('netWt') or 0
        gross = item.get('grossWt') or 0
        total_net_weight += float(net)
        total_gross_weight += float(gross)
- DATABASE MAPPING: Ensure 'itemNos' and 'boxNos' are stored as the strings provided by the frontend.

### 2. FRONTEND MODIFICATIONS (add.html)
- UI CONSISTENCY: Keep the current 'table-group' styling but ensure every field (L, W, H, Net, Gross) is mapped 1:1 to the 'packingGroups' array index.
- COPY FEATURE: Ensure the '📋' button (copy-desc-btn) correctly triggers:
    packingGroups[index].description = packingGroups[index - 1].description;
    renderGroups();
- INPUT SYNC: Ensure all 'addEventListener("input")' calls correctly update the following keys in the array:
    'itemNos', 'boxNos', 'description', 'qty', 'l', 'w', 'h', 'netWt', 'grossWt'.

### 3. DATA INTEGRITY (Grouping Prevention)
- SPELLING LOCK: The purpose of the Copy Icon is to ensure 'Material & Packaging Description' is IDENTICAL across rows. 
- REASONING: Different spellings (e.g., "Pump" vs "Pumps") cause split grouping in the final PDF/View. The AI must ensure the Copy button is the primary way to duplicate descriptions to prevent typos.

### 4. PAYLOAD STRUCTURE
The JSON sent to the server must look like this:
{
  "packingListNo": "...",
  "items": [
    {"itemNos": "1", "boxNos": "1", "description": "Same Text", "netWt": "10", ...},
    {"itemNos": "2", "boxNos": "1", "description": "Same Text", "netWt": "15", ...}
  ]
}