OBJECTIVE

Change the **column order** in the **Packing List detail table**.

ONLY reorder columns.
NO content changes.
NO rowspan logic changes.
NO loop changes.

---

CURRENT COLUMN ORDER (LEFT → RIGHT)

1. PO / Item No.
2. Material & Packing Description
3. Box No.
4. Qty
5. Dimension
6. Net Weight
7. Gross Weight

---

REQUIRED COLUMN ORDER (LEFT → RIGHT)

1. PO / Item No.
2. **Box No.**
3. **Material & Packing Description**
4. Qty
5. Dimension
6. Net Weight
7. Gross Weight

---

EXACT CHANGES TO APPLY

### TABLE HEADER (`<thead>`)

Move **Box No.** column to be the **2nd column**.
Move **Material & Packing Description** column to be the **3rd column**.

No renaming.
Only reorder `<th>` elements.

---

### TABLE BODY (`<tbody>`)

Reorder `<td>` output to match header order.

Rules:

* `PO / Item No.` stays FIRST
* `Box No.` must render SECOND
* `Material & Packing Description` must render THIRD
* Existing `rowspan` behavior MUST remain intact
* `loop.first` logic MUST remain intact
* `description_merged` logic MUST remain intact

---

IMPORTANT CONSTRAINTS (DO NOT VIOLATE)

* Do NOT change:

  * variable names
  * rowspan calculations
  * grouping logic
  * totals row
  * styling
* Do NOT add or remove columns
* Do NOT touch any other section of the template

---
