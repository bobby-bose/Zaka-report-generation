    <!-- LEFT SIDE (goods description) -->
    <!-- <td width="50%" style=" vertical-align:top; padding:0;">
        <table style="width:100%; border-collapse:collapse;">
            <tr class="bold">
                <th class="left" width="15%">Shipping Mark</th>
                <th class="left" width="20%">No & Kind of Pkgs</th>
                <th class="left" width="65%">Description of Goods</th>
            </tr>
           

            {% for row in tableRows %}
            <tr>
                <td class="left">{{ loop.index }}</td>
                <td class="left">
                    {% if loop.index == (loop.length // 2) + (loop.length % 2) %}
                        AS ADDRESS
                    {% endif %}
                </td>
                <td class="left">
                    {{ row.from }} - {{ row.to }}<br>{{ row.description }}
                </td>
            </tr>
            {% endfor %}
             <tr>
                <td colspan="3" style="text-align: end;">TOTAL ($US)</td>

            </tr>
        </table>
    </td> -->